ข้อ1
- ใช้ฟังก์ชันรับค่าเข้ามาแล้ว จะreturnค่าจะออกมาอย่างเดียว ห้ามมีconsole.logด้านใน
- ใช้arrayเก็บเกณท์การตัดเกรด แล้วใช้findในการค้นหาเกรดที่ตรงกับช่วงของคะแนน ทำให้ไม่ต้องเขียนเงื่อนไขซ้อนกันหลายชั้น
- การตั้งค่าเริ่มต้น หากมีการส่งค่าundefined เข้ามาจะถึงจะดึงค่าเริ่มต้นมาใช้ในการคำนวณ

ข้อ2
- student? .contact?.email ใช้ด฿งข้อมูลแบบเชิงซ้อน ช่วยในการป้องกันโปรแกรมพัง ถ้าไม่เจอข้อมูลในชั้นนั้น
- ?? ใช้ในการกำหนดค่าสำรองล่วงหน้า และจะทำงานเมื่อค่าเป็น nullหรือ undefined

ข้อ3
- mapจะใช้ในการแปลงโครงสร้างข้อมูลทีละตัว
- filter ใช้ในการคัดกรองเฉพาะข้อมูลที่ผ่านเงื่อนไขแล้ว
- reduce ทำหน้าที่รวบรวมข้อมูลทั้งarray ให้เหลือเพียงค่าเดียว