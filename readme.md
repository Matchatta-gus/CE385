ข้อ 1

const nickname = "ซูกัส"; ถึง const registeredCourses = 6;: ใช้ const เพราะเป็นข้อมูลส่วนบุคคลที่กำหนดแล้วไม่มีการเปลี่ยนแปลงค่าใหม่ภายในโปรแกรม ช่วยป้องกันไม่ให้เผลอเขียนโค้ดไปทับค่าเดิม
const currentYear = 2569; และ yearsRemaining = 2;: แยกปีปัจจุบันและจำนวนปีที่เหลือออกจากกัน เพื่อนำไปใช้คำนวณผ่านตัวแปร แทนการเขียนตัวเลขผลลัพธ์ลงไปตรงๆ
ถ้าเจอเครื่องหมายนี้ภายใน Backtick จะประมวลผลสิ่งที่อยู่ข้างในก่อน หากเป็นตัวแปร เช่น ${nickname} จะดึงค่ามาวาง หากเป็นนิพจน์คณิตศาสตร์ เช่น {currentYear + yearsRemaining} จะทำการบวกเลข แล้วแปลงผลลัพธ์เป็นข้อความมาต่อให้อัตโนมัติ

ข้อ 2

String ("สวัสดี"): ข้อมูลข้อความ 
Number (24): ข้อมูลตัวเลข นำไปคำนวณทางคณิตศาสตร์ได้ทันที
Boolean (true): ค่าทางตรรกศาสตร์ มีเพียง true หรือ false
Undefined (let undefinedData;): ตัวแปรที่ถูกประกาศไว้ แต่ยังไม่ได้ถูกกำหนดค่าใดๆ ลงไป
Null (const nullData = null;): ค่าว่างเปล่าที่ผู้เขียนเจตนากำหนดให้ตัวแปรนั้นไม่มีค่า
Array ([1, 2, 3]): ชุดข้อมูลแบบลำดับ
Number(inputAge) + 5: ฟังก์ชัน Number() จะแปลง String "20" ให้กลายเป็น Number 20 เมื่อนำไป + 5 จึงได้ผลลัพธ์เป็น 25 (หากไม่แปลงชนิด ตัวเชื่อม + จะมองเป็นการนำข้อความมาต่อกัน ได้ "205")
scoreNum.toFixed(1): แปลงตัวเลขทศนิยมให้ปัดเศษตามตำแหน่งที่กำหนด ในที่นี้จะได้ "85.5" (ส่งกลับค่าเป็น String)=== vs ==: เครื่องหมาย === จะตรวจสอบ 2 ขั้นตอน คือ 
1) ตรวจชนิดข้อมูล และ 2) ตรวจสอบค่า"20" === 20 ชนิดข้อมูลไม่ตรงกัน จึงตอบ false ทันทีNumber("20") === 20  แปลงเป็น Number ทั้งคู่แล้ว ค่าเท่ากัน จึงตอบ true

ข้อ 3

const WORKSHOP_MAX_RAW = 60; เป็นการตั้งชื่อให้ตัวเลขดิบ
(workshopRaw / WORKSHOP_MAX_RAW) * WORKSHOP_WEIGHTED_MAX 
 หารเพื่อหาอัตราส่วนของคะแนนที่ทำได้ก่อน (48 / 60 = 0.8) จากนั้นคูณด้วยสัดส่วนเต็มของวิชา (0.8 \times 20 = 16) ได้คะแนนปรับฐานเป็น 16 คะแนน
totalScore = workshopScore + attendance + project + midterm + final
 นำคะแนนที่ปรับฐานแล้วบวกรวมกับคะแนนส่วนอื่นๆ ได้คะแนนรวมดิบ
 scoreNeededForA = TARGET_GRADE_A_SCORE - totalScore
 นำเกณฑ์เป้าหมาย (80) ลบด้วยคะแนนรวมที่ได้จริง หากคะแนนรวมยังไม่ถึง 80 จะได้ค่าเป็นบวก (แสดงจำนวนคะแนนที่ขาด) หากเกิน 80 จะได้ค่าติดลบ

ข้อ 4

if (score < 0 || score > 100) {
  return "คะแนนไม่ถูกต้อง ต้องอยู่ระหว่าง 0-100";
} 
 โค้ดส่วนนี้ หากข้อมูลที่ส่งเข้ามาผิดเงื่อนไข (ติดลบ หรือ เกิน 100) ตัวฟังก์ชันจะ return ข้อความแจ้งเตือนและ จบการทำงานทันที โดยไม่ลงไปรันโค้ดตัดเกรดด้านล่าง ช่วยประหยัดทรัพยากรการประมวลผล
if...else if จะประมวลผลจากเงื่อนไขแรกบนสุดลงมาล่างสุด เมื่อเจอเงื่อนไขใดที่เป็นจริง (True) จะรันคำสั่งข้างในแล้วข้ามเงื่อนไขที่เหลือทันที
เมื่อส่งคะแนน 85 เข้ามา เช็ค >= 80 เป็นจริง ได้เกรด A (ถูก)
ถ้าส่งคะแนน 85 เข้ามา เช็ค >= 50 เป็นจริงทันที  คืนค่าเกรด D ทันที ซึ่งผิดพลาดทันที

ข้อ 5

case "ข้าวผัด":
case "ข้าวมันไก่":
case "ข้าวหมูแดง":
  return 50;
เมื่อ JavaScript เจอ case ที่ตรงกัน มันจะรันโค้ดไปเรื่อยๆ จนกว่าจะเจอคำสั่ง break หรือ return
ถ้าเขียนคำสั่งไว้ใน case "ข้าวผัด" และ case "ข้าวมันไก่" จะทำให้โปรแกรม "ไหลผ่าน" (Fall-through) ลงมารวมกันที่ case "ข้าวหมูแดง" และส่งคืนค่า 50 เหมือนกันทั้งหมด ช่วยลดการเขียนโค้ดซ้ำซ้อน
ในทุก switch จะมี default เพื่อทำหน้าที่เป็นทางเลือกสุดท้ายหากข้อมูลที่รับมาไม่ตรงกับ case ไหนเลย
for...of จะคำนวณราคาแต่ละรายการ itemTotal = ราคาพื้นฐาน × ตัวคูณขนาด × จำนวน จากนั้นนำไปบวกสะสมเข้าตัวแปร grandTotal ด้วยตัวดำเนินการ +=

ข้อ 6

login(inputUser, inputPass, role, isActive, age) เมื่อเจอเงื่อนไขที่ไม่ผ่าน โปรแกรมจะ return ข้อความแจ้งเตือน และ เลิกทำงานทันที ไม่ลงไปรันบรรทัดถัดไป

if (inputUser !== validUser || inputPass !== validPass) {
  return "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง";
} 
 ใช้เครื่องหมาย !== ร่วมกับตัวดำเนินการทางตรรกศาสตร์ || ถ้าชื่อผู้ใช้ไม่ตรง หรือ รหัสผ่านไม่ตรง ให้ตีกลับทันที

if (!isActive) {
  return "บัญชีนี้ถูกระงับการใช้งาน";
} 
 ใช้ตัวดำเนินการ !เพื่อกลับค่า Boolean ถ้า isActive ไม่เป็นจริง หมายความว่าบัญชีนี้โดนบล็อก ให้แจ้งเตือนและหยุดทำงาน

if (age < 18) {
  return "อายุไม่ถึงเกณฑ์";
}  
 เช็คเงื่อนไข หากอายุน้อยกว่า 18 ปี จะไม่ยอมให้เข้าใช้งานระบบ

if (role === "อาจารย์") {
  return "เข้าสู่ระบบสำเร็จ (สิทธิ์ผู้ดูแล)";
} else if (role === "นักศึกษา") {
  return "เข้าสู่ระบบสำเร็จ (สิทธิ์ทั่วไป)";
} else {
  return "บทบาทผู้ใช้งานไม่ถูกต้อง";
}
 เมื่อผ่าน 3 อันแรกมาแล้ว แสดงว่าเป็นผู้ใช้จริง บัญชีปกติ และอายุถึงเกณฑ์
สุดท้ายคือเช็ค role ด้วย === เพื่อแจกจ่ายสิทธิ์การใช้งาน ให้ถูกต้องตามประเภทผู้ใช้

ข้อ 6 ส่วนที่ 3 (ถามตอบ)

1.ตอบ ต้องยืนยันตัวตนให้ผ่านก่อนว่าคือใคร แล้วค่อยตรวจสอบสิทธิของคนนั้น
2.ตอบ ทำให้หลุดบอกข้อมูลระบบแก่ผู้ไม่หวังดี โดยที่ยังไม่ได้ใส่รหัสผ่านที่ถูกต้อง